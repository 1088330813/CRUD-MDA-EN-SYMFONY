
{% extends 'base.html.twig' %}


{% block body %}
<a href="{{ path('persistir')}}">Registra las Canciones</a>
<br>
<br>
<a href="{{ path ('paginador', {'genero':'Rock'}) }}">Ver listado de Canciones</a>
    <h1>Consulta la Canción que Deseas:</h1>
   <br />
   {{ form_start(form1) }}
   {{ form_end(form1) }}
  {% endblock %}